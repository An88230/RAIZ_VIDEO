# short-video-maker Runtime Discovery

This document was generated by `scripts/discover-short-video-maker.sh` from a read-only scan of `vendor/short-video-maker`.

Safety boundary:
- No dependencies were installed.
- Docker was not started.
- short-video-maker was not started.
- No network request was made by this discovery.
- No file inside `vendor/` was modified.

## Package

- Name: `short-video-maker`
- Version: `1.3.4`
- Description: Creates short videos for TikTok, Instagram Reels, and YouTube Shorts using the Model Context Protocol (MCP) and a REST API.
- Binary: `short-video-maker` -> `dist/index.js`

## Package Scripts

- `build: rimraf dist && tsc --project tsconfig.build.json && vite build`
- `dev: vite build --watch | node --watch -r ts-node/register src/index.ts`
- `start: node dist/index.js`
- `test: vitest`
- `prepublishOnly: npm run build && echo "#!/usr/bin/env node $(cat dist/index.js)" > dist/index.js && chmod +x dist/index.js`
- `publish:docker: npm run publish:docker:normal && npm run publish:docker:cuda && npm run publish:docker:tiny`
- `publish:docker:cuda: docker buildx build --platform linux/amd64 -t gyoridavid/short-video-maker:latest-cuda -t gyoridavid/short-video-maker:${npm_package_version}-cuda -f main-cuda.Dockerfile --push ./`
- `publish:docker:normal: docker buildx build --platform linux/amd64,linux/arm64 -t gyoridavid/short-video-maker:latest -t gyoridavid/short-video-maker:${npm_package_version} -f main.Dockerfile --push ./`
- `publish:docker:tiny: docker buildx build --platform linux/amd64,linux/arm64 -t gyoridavid/short-video-maker:latest-tiny -t gyoridavid/short-video-maker:${npm_package_version}-tiny -f main-tiny.Dockerfile --push ./`
- `ui:dev: vite`
- `ui:build: vite build`
- `ui:preview: vite preview`

## Runtime Ports

- .env.example PORT=3123
- default config port: 3123
- docker-compose.yml "3123:3123"
- rest.http host: http://localhost:3123

## API Routes

- `DELETE /api/short-video/:videoId (src/server/routers/rest.ts)`
- `DELETE /api/short-video/{{videoId}} (rest.http)`
- `GET * (src/server/server.ts)`
- `GET /api/music-tags (rest.http)`
- `GET /api/music-tags (src/server/routers/rest.ts)`
- `GET /api/music/:fileName (src/server/routers/rest.ts)`
- `GET /api/short-video/:videoId (src/server/routers/rest.ts)`
- `GET /api/short-video/:videoId/status (src/server/routers/rest.ts)`
- `GET /api/short-video/{{videoId}}/status (rest.http)`
- `GET /api/short-videos (rest.http)`
- `GET /api/short-videos (src/server/routers/rest.ts)`
- `GET /api/tmp/:tmpFile (src/server/routers/rest.ts)`
- `GET /api/voices (rest.http)`
- `GET /api/voices (src/server/routers/rest.ts)`
- `GET /health (src/server/server.ts)`
- `GET /mcp/sse (src/server/routers/mcp.ts)`
- `POST /api/short-video (rest.http)`
- `POST /api/short-video (src/server/routers/rest.ts)`
- `POST /mcp/messages (src/server/routers/mcp.ts)`

## MCP Tools

- `create-short-video`
- `get-video-status`

## Docker And Compose Files

- `.dockerignore`
- `docker-compose.yml`
- `main-cuda.Dockerfile`
- `main-tiny.Dockerfile`
- `main.Dockerfile`

## Environment Files

- `.env.example`

## Environment Variables

Detected from `.env.example`:

- `PEXELS_API_KEY=`
- `LOG_LEVEL=trace`
- `WHISPER_VERBOSE=true`
- `PORT=3123`
- `DEV=true`
- `DATA_DIR_PATH=`

Detected from `src/config.ts`:

- `CONCURRENCY`
- `DATA_DIR_PATH`
- `DEV`
- `DOCKER`
- `KOKORO_MODEL_PRECISION`
- `LOG_LEVEL`
- `PEXELS_API_KEY`
- `PORT`
- `VIDEO_CACHE_SIZE_IN_BYTES`
- `WHISPER_MODEL`
- `WHISPER_VERBOSE`

## Startup Behavior Signals

- Requires configuration validation before server start.
- Requires PEXELS_API_KEY for normal startup.
- Initializes Kokoro TTS on startup.
- Initializes Whisper on startup.
- Initializes Remotion on startup.
- Initializes FFmpeg on startup.
- Initializes Pexels API integration on startup.
- Non-Docker startup may run an installation self-test including a Remotion test render.
- Non-Docker startup self-test may call Pexels.
- Serves built UI/static files from dist/ui and static.

## RAIZ Integration Notes

- The primary REST creation endpoint appears to be `POST /api/short-video`.
- Status polling appears to be `GET /api/short-video/:videoId/status`.
- Binary output retrieval appears to be `GET /api/short-video/:videoId`.
- The default upstream port is `3123`.
- Startup requires `PEXELS_API_KEY` and initializes video-generation dependencies, so RAIZ should not start this process implicitly.
- For RAIZ, this repository remains reference-only until an explicitly guarded real integration phase.
